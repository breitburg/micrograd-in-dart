name: micrograd
description: A tiny scalar-valued autograd engine
version: 1.0.0
repository: https://github.com/breitburg/micrograd-in-dart/

environment:
  sdk: ^3.7.2

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
